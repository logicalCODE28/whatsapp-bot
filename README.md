# WhatsApp Bot

A simple WhatsApp bot built with `whatsapp-web.js`.

## Installation

1. Clone the repository.
2. Run `npm install`.
3. Start the bot with `node index.js`.
4. Scan the QR code with your WhatsApp app.

## Features

- Automatic session management (ignored in git for security).
- Extensible command handling.
